# aurum-data 云函数

`aurum-data` 为望潮小程序自动读取已经公开发布并脱敏的最新市场快照：

1. 私有数据仓库在交易日自动抓取并通过审核后发布到固定 GitHub Pages 地址。
2. 小程序打开页面时调用本函数；函数以 10 分钟内存缓存读取最新公开 JSON。
3. 函数再次执行小程序专用脱敏，拒绝数量不足、状态错误或结构异常的半成品。
4. 上游短暂不可用时，小程序保留随版本打包的最后一份已验证快照，页面不会空白。

函数不需要密钥、数据库集合、付费扩展或 request 合法域名。下拉刷新会强制检查公开快照；正常页面跳转共用客户端缓存，避免重复请求。
